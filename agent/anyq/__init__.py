"""Anyq agent package: configuration, prompt text and language helpers.

Extracted from science_manim_graph_agent.py / agent_ws_client.py so that the
constants and the large text blocks live in one place. Import the submodules
directly (``from anyq.config import ...``); this module has no side effects of
its own.
"""
