# The worker only needs a Mongo driver: ffmpeg comes from the image, and the
# job payloads are plain dicts. python-pptx is here for the teacher deck.
motor==3.7.1
pymongo==4.15.3
python-pptx==1.0.2
Pillow==12.3.0
